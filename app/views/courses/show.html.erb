<div>
	<%= @course.course_name %>
	<% if teacher? %>
		<%= link_to 'New Task', new_course_task_path(@course) %>
	<% end %>
	
	<div>
		<% @tasks.each do |task| %>
			<%= link_to task.task_name, course_task_path(@course, task.id) %><br>
		<% end %>		
	</div>

</div>
